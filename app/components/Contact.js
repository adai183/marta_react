var React = require('react');

var Contact = React.createClass({
    render : function () {
        return (
            <div>
                Hello from Contact !
            </div>
        );
    }
});

module.exports = Contact;
